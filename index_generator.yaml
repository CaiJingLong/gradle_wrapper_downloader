index_generator:
  exclude:
    - "**.g.dart"
    - "src/command/**"
    - "src/version.dart"
    - "src/config/cli_config.dart"
  # Define the paths of the folders in which to generate the index files
  indexes:
    - path: lib
# Use `index_generator` or `dart pub global run index_generator` to generate the index files
